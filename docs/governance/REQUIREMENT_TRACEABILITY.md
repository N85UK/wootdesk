# Requirement Traceability

Document ID: `WOOT-TRACE-001`

Last reviewed: 1 September 2026

| ID | Requirement | Implementation evidence | Verification evidence | Status |
|---|---|---|---|---|
| REQ-CONN-001 | Add and validate a Chatwoot server through `GET /api/v1/profile` | `AddConnectionView`, `ConnectionViewState`, `ChatwootAPIClient` | API, URL, decoding, and connection-flow tests | Implemented |
| REQ-CONN-002 | Select an account when a profile has several accounts | `AccountPickerView`, profile DTO mapping | Single-account and multi-account decoding tests | Implemented |
| REQ-SEC-001 | Store every token only in Apple Keychain | `KeychainCredentialStore`, profile UUID lookup | Credential fake lifecycle and profile-deletion tests | Implemented |
| REQ-PROFILE-001 | Restore, switch, revalidate, edit, and remove profiles | `AppModel`, `FileServerProfileRepository`, connection views | Persistence, switching, and deletion tests | Implemented |
| REQ-CONV-001 | Load and display a real conversation list | `ConversationListState`, `ConversationListView`, API client | Conversation decoding, request, paging, and state tests | Implemented |
| REQ-ERR-001 | Present clear network, authentication, rate, and decoding errors | `APIError`, feature state error mapping | HTTP, timeout, offline, malformed-response tests | Implemented |
| REQ-AI-001 | Keep AI as an app-side protocol and mock only | `AIProvider`, research models, `MockAIProvider` | Source review and build | Implemented |
| REQ-DIST-001 | Provide valid platform app icons | `AppIcon.appiconset` | Debug and Release asset-catalog validation on both platforms | Verified |
| REQ-PRIV-001 | Disclose current processing and declare no tracking | `PRIVACY.md`, `PrivacyInfo.xcprivacy` | Source and release-binary review | Implemented, release review pending |
| REQ-DIST-002 | Document App Store preparation and release gates | App Store, metadata, branding, and readiness documents | Documentation review | Implemented |
| REQ-MSG-001 | Show message history for a selected conversation | `ConversationDetailState`, `ConversationDetailView`, message DTO mapping, cursor paging | Mocked request, decoding, state, and UI journey tests; dedicated-server acceptance pending | Implemented, release review pending |
| REQ-REPLY-001 | Send an agent reply or private note | `ConversationComposerView`, `ConversationDetailState`, `ChatwootAPIClient.createMessage` | Public and private request-body tests, draft-safety state tests, and UI journey; live acceptance pending | Implemented, release review pending |
| REQ-ATTACH-001 | Upload and present attachments without automatic remote fetch | `ConversationAttachment`, `ConversationAttachmentView`, multipart API client, composer file importer | Multipart request, tolerant decoding, unsafe-URL, state-retention, and platform build tests; live acceptance pending | Implemented, release review pending |
| REQ-MSG-SAFE-001 | Present processed HTML and inline Markdown without activating embedded links | `MessageTextFormatter`, `ConversationMessageRowView` | Formatting and unsafe-link unit tests | Implemented |
| REQ-COMPAT-001 | Provide an opt-in live compatibility harness that normal CI cannot trigger | `ChatwootLiveCompatibilityTests`, `script/live_compatibility.sh`, compatibility runbook | Default test run skips all three live cases; script requires two flags before writes | Implemented, matrix execution pending |
| REQ-TRIAGE-001 | Change conversation status, snooze with a future return time, change priority, and assign an agent or team, displaying only the state Chatwoot confirms | `ConversationTriageState`, `ChatwootAPIClient+Triage`, `ChatwootTriageDTO`, `Conversation` triage fields | Triage state tests for confirmed, server-differing, and rejected outcomes; API tests for endpoint paths, request bodies, and read-back confirmation; live acceptance pending | Implemented, live acceptance pending |
| REQ-TRIAGE-002 | Preserve the complete server label set when adding or removing a label | `ConversationTriageState.changeLabels`, `ConversationTriageState.merged`, `ChatwootAPIClient+Triage` label endpoints | Tests proving a label added on the server after display survives an add and a remove, that case-insensitive duplicates are not created, and that the confirmed set comes from the server | Implemented, live acceptance pending |
| REQ-TRIAGE-003 | Reach status, snooze, priority, assignment, and label actions on iPhone, iPad, and Mac without obscuring the message timeline, with keyboard shortcuts, VoiceOver values, and no non-essential motion | `ConversationActionsView`, `ConversationDetailView` | Serialised-submission and confirmed-state unit tests; macOS and iOS Simulator builds; VoiceOver and keyboard acceptance on physical hardware pending | Implemented, assistive-technology acceptance pending |
| REQ-NAV-001 | Present workspace navigation, the conversation list, and the selected conversation as adjacent areas on iPad and Mac, collapsing without losing the selection, and keep filters operable at accessibility text sizes | `WorkspaceSidebarView`, `MainAppView` split layouts, `ConversationListPresentation`, `ConversationFilterPickerLayout`, `ConversationWorkspaceReset` | Workspace-reset, selection-retention, and filter-layout tests across every `DynamicTypeSize`; macOS and iOS Simulator builds; iPad hardware acceptance pending | Implemented, iPad hardware acceptance pending |
| REQ-NOTIFY-ROUTE-001 | Open the conversation a notification identifies, including one outside the loaded page or hidden by a filter, clearing the previous profile first and never substituting another conversation | `ConversationRouteCoordinator`, `ConversationListState` routing helpers, `AppModel.activateNotificationRoute` | Coordinator tests for out-of-page fetch, filter clearing, profile isolation, account mismatch, unavailable conversation, and repeat activation | Implemented, signed-device acceptance pending |
| REQ-PUSH-CLIENT-001 | Request notification permission, register with APNs, enrol and remove devices, and validate opaque notification routes without pretending delivery is active | `PushNotificationState`, `PushGatewayRegistrationManager`, `PushGatewayAPIClient`, `PushNotificationRoute`, `WootDeskApplicationDelegate`, platform entitlements, notification Settings section | Permission, APNs lifecycle, gateway request, Keychain configuration, rotation, deletion, and cross-profile routing tests; macOS and iOS Simulator builds | Implemented in source, signed-device acceptance pending |
| REQ-PUSH-PROVIDER-001 | Relay new public incoming Chatwoot message events to the correct saved profile through APNs | `Gateway/`, `docs/PUSH_NOTIFICATIONS.md`, ADR 0004 | Gateway authentication, encryption, webhook policy, APNs signing, idempotency, invalid-token, and atomic-store tests; signed entitlements and physical delivery remain | Implemented in source, deployment, recipient policy, and physical acceptance are release blockers |

The final release commit must replace any pending verification entry with exact
test, archive, or review evidence. Live server credentials and customer data are
never acceptable traceability evidence.
